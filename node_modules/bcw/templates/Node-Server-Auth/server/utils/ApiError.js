export default class ApiError extends Error {
  /**
   * Creates an Api Error message and optional status (default 400) handled in the controller
   * @param {string} message
   * @param {number} status
   */
  constructor(message, status = 400) {
    super(message);
    this.status = status;
  }
}
